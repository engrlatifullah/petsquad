import 'dart:ui';

class RoleModel {
  final String ? icon;
  final String ? title;
  final Color ? color;
  const RoleModel({this.icon, this.title, this.color, });
}

class Medicine {
  final String ? image;
  final String ? name;
  final Color ? color;
  const Medicine({this.color, this.image, this.name, });
}